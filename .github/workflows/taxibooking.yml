name: Taxi-Booking Real-Time Project

on:
    push:
        branches: 
            - main
    workflow_dispatch:
      
jobs:
  build:
    name: runs on ubuntu-latest
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
      - name: build test package
        run: |
          working-directory: ./taxi-booking
          mvn compile